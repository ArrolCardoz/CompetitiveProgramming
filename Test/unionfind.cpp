#include <bits/stdc++.h>
using namespace std;
/*
disjoint sets
eg: unionFind parent(10) --> it make list that points itseld
merge:
merge(0,1) ---> 1 points 0 i.e. 1->0 and 0->0
find(i):
if we find a num then it outputs its parent
eg: if we use the same example as we merged above
find(0)->0
find(1)->0

*/